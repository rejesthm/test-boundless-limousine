# test_repo_example

1. Put your `GOOGLE_MAPS_API_KEY` in `.env` (copy from `.env.example` if needed). I haven't put mine—it's personal. It should work.

2. Run:

   ```
   dart run build_runner clean
   dart run build_runner build --delete-conflicting-outputs
   ```

3. Run the app:
   ```
   flutter run
   ```

Samplee Number
+17744153244
